@GET
    @Path("/getEmp/{eid}")
    @Produces({"application/json", "application/xml"})
    public Emp getEmp( @PathParam("eid") int eid) {
        EmpDAO edao=new EmpDAO();
        Emp e=edao.getEmp(eid);
        if (e == null) {
           // throw new NotFoundException();
            throw new WebApplicationException(Response.Status.NOT_FOUND);
        }
        return e;
    }




@GET
    @Path("/getEmp/{eid}")
    @Produces({"application/json", "application/xml"})
    public Emp getEmp( @PathParam("eid") int eid) {
        EmpDAO edao=new EmpDAO();
        Emp e=edao.getEmp(eid);
        if (e == null) {
            throw new NotFoundException();
        }
        return e;
    }






package com.rest;

import javax.ws.rs.GET;
import javax.ws.rs.Path;
import javax.ws.rs.Produces;
import javax.ws.rs.core.MediaType;
import javax.ws.rs.core.Response;

@Path("/serv")
public class GenService {
    @GET
    @Path("/hello")
    @Produces(MediaType.TEXT_PLAIN)

    public Response services(){
        return Response.status(200).entity("Hello World").build();
    }
}





package com.rest;

import javax.ws.rs.client.Client;
import javax.ws.rs.client.ClientBuilder;
import javax.ws.rs.core.Response;
import java.io.*;
import java.net.*;

public class Main {
    public static void main(String[] args) throws IOException {
        StringBuilder result = new StringBuilder();
        URL url = new URL("http://localhost:8080/WebServices3124/rest/serv/hello");
        HttpURLConnection conn = (HttpURLConnection) url.openConnection();
        conn.setRequestMethod("GET");
        try (BufferedReader reader = new BufferedReader(
                new InputStreamReader(conn.getInputStream()))) {
            for (String line; (line = reader.readLine()) != null; ) {
                result.append(line);
            }
        }
        System.out.println(result.toString());
    }
}





Run Tomcat9
Check in browser:
http://localhost:8080/WebServices3124/rest/serv/hello
Hello World

Now Run Main with coverage:






